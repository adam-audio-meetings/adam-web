import { AudiolistenedDirective } from './audiolistened.directive';

describe('AudiolistenedDirective', () => {
  it('should create an instance', () => {
    const directive = new AudiolistenedDirective();
    expect(directive).toBeTruthy();
  });
});
